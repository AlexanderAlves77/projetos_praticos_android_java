server:
  port: 8888
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AlexanderAlves77/projetos_praticos_android_java/fulldevstacks_config_server
          search-paths: 'greeting-service*'
#          username: username
#          password: password
#        default-label: main
#  profiles:
#    active:
#      - native